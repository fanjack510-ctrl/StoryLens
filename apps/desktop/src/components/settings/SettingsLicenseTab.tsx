import { LicenseSettingsCard } from "./LicenseSettingsCard";

/** Layout tab: delegates VIP / license UI to the settings card. */
export function SettingsLicenseTab() {
  return <LicenseSettingsCard />;
}
