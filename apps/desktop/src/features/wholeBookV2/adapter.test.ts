import{describe,expect,it}from"vitest";import{parseProgressV2,parseWholeBookV2,sevenModules}from"./adapter";
const ev={evidence_id:"e1",snapshot_id:1,revision_hash:"r",chapter_id:101,chapter_index:1,chapter_title:"一",start_offset:0,end_offset:1,quote_or_excerpt:"x",reason:"support"};
const fixture:any={schema_version:"whole-book-analysis-v2.0",book_metadata:{},type_profile:{},overview:{},story:{structure_stages:[]},characters:{protagonist:{stages:[]}},suspense:{lifecycles:[]},pacing:{points:[]},chapters:{heatmap:[]},assessment:{dimensions:[]},evidence_index:{e1:ev},analysis_metadata:{}};
describe("formal Whole-Book V2 adapter",()=>{it("owns seven modules and validates formal data",()=>{expect(sevenModules).toHaveLength(7);expect(parseWholeBookV2(fixture).evidence_index.e1.chapter_id).toBe(101)});it("rejects mock/legacy and chapter identity confusion",()=>{expect(()=>parseWholeBookV2({...fixture,schema_version:"legacy"})).toThrow();expect(()=>parseWholeBookV2({...fixture,evidence_index:{e1:{...ev,chapter_id:1}}})).toThrow()});it("validates actionable progress",()=>{expect(parseProgressV2({schema_version:"whole-book-progress-v2.0",overall_percent:42,current_action:"提取窗口"}).current_action).toBe("提取窗口")})});
