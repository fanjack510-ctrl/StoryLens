# Phase 2A Partial Result Contract

Readable when: ≥1 module-producing stage completed; projection partial/completed; artifact schema/version valid; snapshot consistent.

## Rules

Later failure does not delete existing results; mark partial/mock/candidate; evidence on demand; no auto-canonical; cancelled/interrupted candidates still readable; stale ≠ failed; Result API does not trigger runs.
