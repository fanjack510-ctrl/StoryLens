# 导入第一本书

## 支持格式

TXT、DOCX、EPUB。

## 步骤

1. 打开书库（空库时可直接看到导入引导）。  
2. 选择文件上传。  
3. 确认书名与章节识别结果。  
4. 打开某一章进入阅读器（段落具有稳定 ID）。  

## 注意

- 示例/Demo 书 **默认不会自动导入**。  
- 用户正文只保存在本机数据库，不会进入 Git。  
- 重新解析会有安全确认；正式分析前请先确认章节正确。  

详见历史文档：`docs/15_phase_2a1_ingestion_and_diagnostics.md`。
