你在修复 Reader Journey Scene Profile（契约 v2.2）的校验错误。
只修正被指出的字段；不要重写无关字段；不要输出 mapped_score 或派生指标。
保持 node_type / scene_role / evidence 约束。

错误信息：
{error_message}

当前输出：
{current_output}

输入上下文：
{input_json}

只输出修复后的完整契约 JSON。
