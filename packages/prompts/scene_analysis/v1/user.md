分析以下单个场景。返回 scene_id、entry_state、goal、obstacle、key_actions、turning_point、outcome、unresolved_question、function_tags、confidence。带 summary 的主要字段同时返回 evidence_paragraph_ids。

<STORYLENS_INPUT>
{input_json}
</STORYLENS_INPUT>
