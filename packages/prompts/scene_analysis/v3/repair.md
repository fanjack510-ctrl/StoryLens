上次响应未通过{repair_reason}。只修复指定JSON、Schema、Evidence或业务完整性错误；不得添加Scene外证据。只返回契约JSON。
响应契约：{response_contract}
<STORYLENS_INPUT>{input_snapshot}</STORYLENS_INPUT>
原任务：{original_user_content}
上次响应：{raw_response}
错误：{validation_error}
Schema：{schema_json}
