# StoryLens 1.2.0 产品验收走查

**性质：** 产品验收，不是发布工程门禁
**基线：** 1.2.0（`release/baseline.json` 现为 `status: unverified`、`released_at: null`、`channel: development`）
**成文：** 2026-08-20 · **第二轮：** 2026-08-20 免费项 · **第三轮：** 2026-08-20 付费项 · **第四轮：** 2026-08-20 修复 D1/D3 并复验 · **第五轮：** 2026-08-20 构建 1.2.0 并免安装装机验收 · **第六轮：** 2026-08-20 用户实操暴露三处新缺陷

---

## 为什么需要这份东西

`release/acceptance/1.0.4.md` 与 `1.0.5.md` 记的是**发布工程门禁**——自动化测试、Windows 构建、冒烟、数据升级。那两份文档自己写明：

> `verified` / `ready` … 指的是**发布工程门禁**。它们**不声称每一条变更被最终用户逐项人工验收过**。

于是就有了今天这个局面：

| base_version | 变更数 | 未发布 |
|---|---:|---:|
| 1.0.4 | 17 | **0** ← 最后一次真正发出去的 |
| 1.0.5 | 67 | 66 |
| 1.1.0 | 4 | 3 |
| 1.1.1 | 34 | 34 |
| 1.1.2 | 11 | 11 |
| 1.2.0 | 86 | 86 |
| **合计** | **240** | **202** |

1.1.2 那趟车停在 `rc5-built-awaiting-installed-acceptance`。

**每一条变更都单独测过，但没有任何一刻有人把它们装成一个整体、指着说「这就是它」。** 「项目到底能做啥」说不清，不是因为功能多，是因为没有一个「当前版本」存在过。

这份清单就是那个答案的载体：**打勾的那些，就是 1.2.0 能做的事。**

---

## 一、装车面：1.2.0 装什么

用户能碰到的表面就是**书籍工具条上的六个入口，加书库和设置**：

```
章节目录 · 阅读设置 · 作品画像 · 全书分析 · 分析本章 · 更多
```

其余的都已经不在装车面内，且有代码证据：

| 东西 | 为什么不在装车面 | 证据 |
|---|---|---|
| `/dev/*` 四条路由 | 生产构建里剔除 | `router.tsx`：`import.meta.env.DEV ? [...] : []` |
| `/workspace`、`/cases` | 只在开发者模式下出现，默认关 | `DevelopmentNavigationGroup.tsx` |
| `/books/:id/whole-book-insights` | 入口永久返回 `null` | `WholeBookInsightsEntry.tsx`：`return null`，注释「always hidden」 |
| `/books/:id/pro-native-overview` | 受 `VITE_PRO_NATIVE_OVERVIEW_ENABLED` 控制 | `proNativeOverviewFlag.ts` |
| `/books/:id/whole-book-v2` | 仅任务中心跳转，无常规入口 | `TasksPage.tsx:387` |

---

## 二、走查清单

**跑法：在装好的应用里跑，不是在 dev server 里。** 这是 1.1.2 卡住的那一步，也是这份清单唯一不能打折的地方。

图例：**✅ 已验**（我今天在 dev server 里实测过）· **❌ 有缺陷** · **⬜ 待跑**（需要装机版或要花钱）

### A. 导入

| # | 点哪里 | 应该看到 | 现状 |
|---|---|---|---|
| A1 | 书库 → 导入小说，选一个 TXT | 解析面板：文件名、编码、大小、识别到几章、章节列表 | ✅ |
| A2 | 同一面板 | 「这是一部长篇，还是一个短篇？」二选一，默认按建议勾选 | ✅ |
| A3 | 传一个 15 万字以上的文件 | 「短篇」置灰，提示位写「超过 150,000 字，切段装不下」 | ✅ 24 万字样本实测 |
| A4 | 改选另一项后完成导入 | 库里存的是你选的那个，不是建议值 | ✅ 112 字/3 章样本改选长篇，落库 `long` |
| A5 | 传一个章号识别不出的文件 | 黄色告警，说明是五种原因中的哪一种，并列出能识别的格式 | ✅ 告警写明「整本书只切出了一章」，列出六种格式，并留了「本来就不分章的作品可以直接继续导入」的出口 |
| A6 | 传同一个文件两次 | 拒绝，提示重复 | ✅ 接口 409 `DUPLICATE_BOOK`，按内容哈希判定（改文件名骗不过）。面板标题「书籍可能已存在」、正文「该文件已导入」——但中间夹了一句无意义的「无法读取数据」 |
| A7 | 删除一本书 | 章节与段落一并删除，不留孤儿 | ✅ 接口 204，孤儿数 0 |

### B. 书籍工作台

| # | 点哪里 | 应该看到 | 现状 |
|---|---|---|---|
| B1 | 书库 → 打开任一本书 | 工具条六个入口；章节目录；正文可读 | ✅ 书 14（84 章）、书 15（19 章 + 3 条资料） |
| B2 | 章节目录 → 任一章 | 正文切换，上一章/下一章可用 | ✅ |
| B3 | 阅读设置 | 字号、行距可调，且刷新后保持 | ❌ **改得动，刷新就回去** |
| B4 | 更多 | 六项：完整正文、确认场景划分、查看任务记录、章节信息、技术信息、重新识别章节 | ✅ 菜单渲染 |
| B5 | 更多 → 章节信息 / 技术信息 | 各自打开且有内容 | ✅ 章节信息「一梦如初 · 22 个分区」；技术信息「Book ID 15 · Hash cd707e36155b…」 |
| B6 | 一本被标为短篇的书 | 工具条只出现「短篇精读」，「全书分析」不出现 | ✅ 书 15 实测，两个入口互斥 |

### C. 作品画像

| # | 点哪里 | 应该看到 | 现状 |
|---|---|---|---|
| C1 | 工具条 → 作品画像 | 五个轴：变现、读者、引擎、视角、篇幅 | ✅ 16 本中 13 本已确认 |
| C2 | 五轴都选后确认 | 工具条上出现 ✓；未填满时不能确认 | ✅ 「保存画像」置灰，并写明「还有 情感主轴、视角结构 没有选择」；每一轴下附判定依据（章长中位、词表密度、提及占比） |
| C3 | 从单章进入画像页并确认 | 回到原来那一章，而不是被丢进全书分析 | ⬜ 对应用例在单线程下失败，见下 |

### D. 分析本章 / 场景 / 阅读旅程

| # | 点哪里 | 应该看到 | 现状 |
|---|---|---|---|
| D1 | 工具条 → 分析本章 | 开始对话框：模型、预估、确认 | ✅ **已修复**（09b42911）。弹窗完整，跑通到 succeeded |
| D2 | 跑完后 | 结果视图有内容 | ❌ **根因已查明**：结果视图要求 URL 带 `analysisRun`，不会回退到本章最近一次成功的运行 |
| D3 | 更多 → 确认场景划分 | 场景边界可确认/调整 | ✅ **已修复**（09b42911）。列出 5 个场景、可确认并继续 |
| D4 | 阅读旅程页签 | 逐场景的读者旅程曲线 | ❌ **一片空白**，且启动它的唯一入口是静默空操作 |
| D5 | 确认场景后回到面板 | 面板显示分析完成 | ✅ **已修复**（64c3f8d0）。修前永远显示「待确认场景划分」 |
| D6 | 预算不足时开始分析 | 说明差多少、去哪调 | ❌ **只给空白页**，接口其实已备好可读的话 |

**D1 的实测与根因（本轮最重要的一条）：** 「分析本章」的弹窗完整——分析范围、执行方式、三档分析模式、正文发送确认，未刷新服务状态前「开始」正确置灰。但刷新之后仍然是「连接状态：配置尚未完成，暂时无法开始」。抓到 preflight 的原始响应：

```
error_code:         PROVIDER_HEALTH_STALE
health_source:      cached_success
health_checked_at:  2026-08-18T16:57:51Z   ← 两天前
health_ttl_seconds: 86400                   ← 24 小时
```

而此时 provider 是 `credential_state: configured` / `connection_state: connected` / `enabled: true`，设置里点「测试连接」明确回「✓ DeepSeek API 连接正常」，短篇分析几分钟前刚用同一个 provider 跑完两轮。

代码里本来有 `apply_validation_snapshot_health`，设计意图正是让设置里的验证覆盖过期的健康状态。**但验证快照是空的**——「测试连接」成功后不写快照，「保存配置」也不写（保存后错误码从 `PROVIDER_HEALTH_STALE` 变成 `PROVIDER_STATE_CHANGED`，仍然 409）。

**结论：健康缓存 24 小时过期后，「分析本章」永久卡死，而 UI 上没有任何一条路径能解除它。** 这道门只存在于 `analysis.py`（单章路径）；全书分析与短篇精读都不经过它，所以它恰好只杀掉工具条上的那个主按钮。阿里云那个表单**根本没有测试按钮**，所以对该 provider 而言原则上就无解。

**修复（09b42911）：** 「测试连接」改为先跑传输诊断、再发一次真实的 32-token 调用，并改名「验证连接」——因为它现在做的就是验证。实测：点击后写入 `validation_status=success` 与当前时刻的 `validated_at`，此前一直拒绝的 preflight 随即变为「配置完成，可以开始」。

**第二处独立故障（同一提交修复）：** `_get_or_create_partition_session` 写的是 `status="partition_ready"`，而响应模型的 `ReviewStatus` Literal 里没有这个值 → 每次读取 partition-ready 的审阅都 pydantic 校验失败返回 500 → 工作台渲染成「当前章节没有待确认的场景划分」。**一个词没写进枚举，就让所有走到该状态的章节都没法确认场景**——书 14 第 4090 章从 08-16 就卡在那里。

**两处修好后，单章分析完整跑通：** 开始 → 边界审阅（给出 5 个场景并确认）→ succeeded，产出 5 个场景与 5 条 `scene_analysis`。

**同时确认：** 新建的 run 28 依然是 `book_id` / `analysis_type` / `scope_type` / `start_chapter_id` **全为 NULL**——这是**当前行为，不是历史遗留**。

**B3 的实测：** 字号 17px → 点两次「增大字号」变 18px → 刷新回到 17px。`uiStore` 是普通 zustand store，`fontSize: 17` 写死默认值，**没有 persist 中间件**，localStorage 里也没有对应键——阅读设置只活在内存里，每次刷新和每次重启应用都回到默认。

**E2 的实测与根因：** 书 12《穿越之种田难为》39 万字**只切出 2 章**，单章 19.4 万字。`plan_windows` 抛 `ValueError: single chapter exceeds safe context capacity: 194412>106000`，一路冒成 500，前端把它归类成连接失败。**这是分章失败与错误分类两个问题叠在一起：真正的原因是「这本书的章切错了」，用户看到的是「服务连不上」。**

**D2 的根因（第四轮查明）：** 内容体的渲染条件是 `activeTab === "scene" && analysisRunId`，而 `analysisRunId` **只从 URL 的 `analysisRun` 参数取**，代码里有一句明确的注释「Do not auto-write analysisRun into the URL」。

所以 `?chapter=4175&view=result` 渲染出的只有外壳（30 个字符）；补上 `&analysisRun=28` 之后**同一页面立刻完整渲染**——侧栏 5 个场景 S01–S05 带边界标记，正文带段落 ID，共 3,427 字符。

不自动绑定运行这个设计意图是合理的（打开书或切章不该把人丢进一份旧分析）。缺陷在于：**明确请求 `view=result` 却没有绑定运行时，页面既不回退到本章最近一次成功的运行，也不说明为什么是空的。**

**我先前把 D2 归因于「运行记录 book_id 为 NULL」，那是错的**——审阅会话里 `book_id` / `chapter_id` / `analysis_run_id` 都写得好好的。当时我标注了「因果未证实」，现在正式更正。

### E. 全书分析

| # | 点哪里 | 应该看到 | 现状 |
|---|---|---|---|
| E1 | 工具条 → 全书分析（未分析过的书） | 预估面板：块数、调用次数、费用区间；评测/拆文二选一 | ⬜ 未跑。已确认它**不走** D1 那道健康门 |
| E2 | 未确认画像的书 | 画像门挡住，给出去确认的链接 | ❌ **书 12 直接 500**，页面显示「本地分析服务暂时不可用」 |
| E3 | 拆文选项 | 只在长篇引擎的书上可选，其余置灰 | ⬜ 运行时正确，但类型未声明（本次已修） |
| E4 | 已分析完的书 | 七个模块：总览/故事/人物/悬念/节奏/章节/综合诊断 | ✅ 书 1、14、15 |
| E5 | 报告顶部画像卡 | 主类型、副类型、核心叙事驱动力、重点分析方向 | ❌ **四格只有一格来自你确认的五轴** |
| E6 | 导出 PDF · VIP | 出 PDF | ✅ 403 VIP 门，弹窗写明「可在爱发电购买月卡授权…HTML 导出保持免费」并给激活路径。真 PDF 生成需授权码，未验 |
| E7 | 导出 HTML | 出 HTML | ✅ 615 KB，七模块齐全，标题正确——但**含 13 处「与主角的关系 unknown」** |

**E5 的实测：** 《一梦如初》五轴齐全且来源全为 `user`，报告里「副类型／核心叙事驱动力／重点分析方向」**三格全空**（显示破折号）。只有「主类型」是从确认的轴换算来的，另三格靠最后一次模型调用现填。《深海余烬》那本三格是满的——所以这是**看书**，不是必然为空。

### F. 短篇精读

| # | 点哪里 | 应该看到 | 现状 |
|---|---|---|---|
| F1 | 短篇的书 → 短篇精读 | 类型六选一，说明约十次调用、一分半钟 | ✅ |
| F2 | 跑完后 | 一句话梗概、起承转合、情绪走向、反复出现的说法、逐段六列拆稿 | ✅ 书 16，20 段 |
| F3 | 导出这份拆稿 | A4 横排 HTML，20 段全在，六列 + 呼应 | ✅ 14,419 字符 |
| F4 | 超过 15 万字的书 | 入口不出现；直达页面说明为什么 | ✅ |
| F5 | 重新分析 | 默认复用上次分段；勾「重新切分场景」才重切 | ✅ 5 次调用 / 43 秒，分段逐位复用；编造的那条呼应已被丢弃 |

**F5 的实测：** 重跑两次（读法 6、7），都是 5 次调用、20 段、分段与上一次逐位相同。编造的「呼应第12段夹克男欠账」已被丢弃，两次的呼应离线复检 **0 条不合格**。

**但呼应产量波动很大：** 同一篇、同一套分段，三次运行存活的呼应数是 **16 → 8 → 14**。校验本身是稳定的（把读法 5 的 16 条放到同一套分段上离线判，仍是留 15 丢 1），波动来自模型每次写出的呼应质量不同。

**顺带补了一个计数器**（本轮改动）：两道呼应校验此前都是**把字段清空、不留记录**，所以「模型少写了」和「校验吃掉了」在结果里长得一模一样。现在 `callbacks_written / dropped_bad_target / dropped_bad_content` 会写进日志。

**F2 的已知偏差：** 库里那份读法**早于呼应内容校验**（今天才提交），所以第 16 段那条编造的「呼应第12段夹克男欠账」仍在里面。重跑一次才会被丢掉。

### G. 设置与凭据

| # | 点哪里 | 应该看到 | 现状 |
|---|---|---|---|
| G1 | 设置 → 模型服务商 | 可配置、密钥存 keyring、不回显明文 | ✅ 密钥 `type=password`，页面写明「保存后不会回显 API Key」 |
| G2 | 未配置密钥时开始分析 | 明确报错，不是静默失败 | ⬜ |
| G4 | 设置 → 验证连接 | 验证成功后，单章分析能开始 | ✅ **已修复**（09b42911）。按钮改名「验证连接」，现在真发一次 32-token 调用并写入验证快照 |
| G3 | 设置 → 隐私与更新 | 检查更新按钮存在 | ⬜ **网页版本来就没有这个按钮**（该栏写「网页版更新随本地 StoryLens 服务更新」）。只能在装机版验证——那条失败的测试是环境错配 |

### H. 装机与升级

| # | 做什么 | 应该看到 | 现状 |
|---|---|---|---|
| H1 | 装 Windows 安装包 | 装得上，能启动 | ✅ **构建成功并免安装跑通**：`StoryLens_1.2.0_x64-setup.exe`（42.7 MB）。应用启动、拉起自带 sidecar、22 条迁移全跑齐、完整跑通一条短篇线 |
| H2 | 用 1.0.x 的库升级 | 迁移跑通，旧书旧结果都在 | ⬜ 未做——需要覆盖你在用的 1.1.2 并对 920MB 生产库跑迁移，等你决定 |
| H3 | 断网启动 | 本地功能可用，联网功能给出可读的错误 | ⬜ |

---

## 三、走查中发现的缺陷

按「用户能不能撞上」排。

1. ~~**「分析本章」硬阻塞**~~ **已修复**（09b42911）。
2. ~~**场景划分确认打不开**~~ **已修复**（09b42911）。
3. **单章分析结果视图空白**（D2）。根因已查明：`view=result` 不带 `analysisRun` 就只渲染外壳，且不回退到最近一次成功运行。
4. **超大章的书打开全书分析直接 500**（E2），且被显示成「本地分析服务暂时不可用」。分章失败的后果被伪装成网络问题。
3. **阅读设置不持久**（B3）。改了字号/行距/宽度，刷新即失效。
4. **报告画像卡四格只有一格来自你确认的五轴**（E5）。你花力气确认了五个轴，报告用上一个。
5. **书名一旦导入错就永远错**。取自文件名、不校验、产品里没有任何改名入口，而它出现在每个页面标题和每份导出里。
6. **前端测试套件并行执行会掩盖失败**。同口径单线程重跑，**HEAD 就有 13 个失败**（并行只显示 5 个）。C3 与 G3 就在其中，而 G3 已查明是环境错配而非产品缺陷——其余的需要在装机验收之前逐条查清。
7. **`whole_book_enhanced` 枚举仍在四处**，无产品语义。
8. **超大章被切成多段后仍共用一个 `chapter_ref`**。
9. **块缓存没有清理和容量上限。**
10. **短篇分析完全不进用量账本。** `short_form_results` 记了 `provider_calls: 5`，但 `input_tokens` / `output_tokens` 都是 0，`model_invocations` 里一条都没有（最后一条仍停在 08-18）。今天两轮短篇跑完，账本上看不出花过钱——而产品有预算设置和费用预估。
11. **HTML 导出里 13 处「与主角的关系 unknown」。** `relationship_to_protagonist` 的原始枚举值直接上屏。
12. **「分析本章」弹窗写着「全书（后续开放）…全书分析将在后续版本开放」**，而全书分析早已是工具条上的正式入口。文案与产品现状矛盾。

---

## 四、明确不在这次装车面内

- **反转未被命名** —— 需要模型维护「读者当前应当相信什么」这一状态，不是改提示词能解决的。
- **拆文的独立验收** —— 需要一本有公开人写拆解、且引擎没被那份拆解污染过的书。在这条补上之前，所有拆文改动的有效性都无从判断。
- **跨读法复用 L1** —— 已实测否决并关闭（CHG-119）。
- **30k–80k 字的结构塌陷** —— 导入时选长篇/短篇已经绕开了分流的那条缝（CHG-120），但全书引擎在短书上仍然只解出两个阶段。

---

## 五、怎么用这份清单

1. **先把 ⬜ 跑掉。** 优先 H1——在装好的应用里跑，而不是 dev server。其余的 ⬜ 按 A→H 顺序走。
2. **每跑一条，就地改成 ✅ 或 ❌。** ❌ 要写清「看到的是什么」，不要写「不工作」。
3. **全部走完，这份文件就是 1.2.0 的产品定义。** 「项目能做啥」＝ 清单上 ✅ 的那些。
4. **然后才谈发布。** 发布工程门禁（测试/构建/冒烟）按 `1.0.5.md` 的格式另出一份，两份都齐了才动 `baseline.json`。


---

## 六、第二轮走查记录（2026-08-20）

免费项已全部跑完，结果如上表。合计 **✅ 11 条新增通过 · ❌ 3 条新增缺陷 · ⬜ 剩余全部需要花钱或装机版**。

剩下的 ⬜ 分两类：

**要花钱**（各跑一次真书）：D1 分析本章 · E1 全书分析预估与开跑 · E6/E7 报告导出 · F5 短篇重新分析
**要装机版**：G3 检查更新 · H1 安装启动 · H2 数据升级 · H3 断网

**一条走查方法上的更正：** 第一轮我把 sidecar 掉线导致的空白页误判成「结果视图缺陷」。现在 sidecar 用 PowerShell 脱离进程树启动，不会再被后台任务超时回收；D2 的结论是在 sidecar 健康、请求全部 200 的前提下复验过的。

**一条自伤记录：** 我曾用裸 `sqlite3` 执行 `delete from books where id=17` 清理测试数据。SQLite 默认不开外键约束，于是留下 5 条段落和 3 个章节的孤儿行；而段落主键是按 book id 拼的（`B0017-C0001-P0001`），下一本书拿到 17 号就主键冲突、导入 500。**这不是产品缺陷，是我造成的。** 已清理，并改用 `DELETE /api/v1/books/{id}`（实测干净，不留孤儿）。教训：这个库的清理只能走接口。


---

## 七、第三轮走查记录（2026-08-20，付费项）

**实际花费：** 两轮短篇分析，各 5 次调用，对象是一篇 8,577 字的短篇。**精确金额给不出来**——见缺陷 10，短篇的 token 用量根本没进账本。按同规模历史数据估算约 ¥0.05–0.1。

**本轮结论：**

| 项 | 结果 |
|---|---|
| E6 导出 PDF | ✅ VIP 门正确，弹窗清楚 |
| E7 导出 HTML | ✅ 产出完整，但含 13 处 `unknown` |
| F5 短篇重新分析 | ✅ 复用分段、丢弃编造呼应 |
| D1 分析本章 | ❌ **硬阻塞，且 UI 无解** |
| E1 全书分析开跑 | ⬜ 未跑（D1 的门与它无关，已确认） |

**D1 是这次走查最重要的发现。** 它不是「某个页面不好看」，是**工具条上的主按钮在闲置 24 小时后就永久失效，而用户能点的每一个按钮都解不开它**。在装机验收之前，这一条应该排在所有其他缺陷前面。

**方法上的一条记录：** 我一度以为 D2（结果视图空白）能靠跑一次新的单章分析来判断是数据问题还是视图问题。D1 阻塞之后这条路断了——**D2 现在无法证伪，要等 D1 修好**。


---

## 八、第四轮：修复 D1 与 D3（2026-08-20）

**修的两处（提交 09b42911）：**

1. **「测试连接」→「验证连接」**：先传输诊断，再发一次真实的 32-token 调用。只有真实调用会写验证快照，而那正是 preflight 唯一采信的证据。
2. **`ReviewStatus` 补上 `partition_ready`**：写入方一直在写这个值，读取方的 Literal 里没有，于是每次读都 500。

**复验：** 单章分析从零跑通——开始 → 5 个场景的边界审阅 → 确认 → succeeded，产出 5 条 `scene_analysis`。这是这个库里**第一次有一条完整、带 book/chapter 归属、可复现的单章分析**。

**没修的：** D2。根因已经查得很清楚（缺一个「回退到本章最近成功运行」），但它牵涉 URL 与运行绑定的既有设计意图，值得单独一次改动，不该混在这次修复里。

**方法上的一条更正：** 我曾把 D2 归因于「运行记录的 `book_id` 为 NULL」。**错了**——审阅会话的归属字段完好，D2 是纯前端的 URL 绑定问题。当时标了「因果未证实」，这里正式更正。`book_id` 全 NULL 是另一件独立的事，且已确认是**当前行为**而非历史遗留。

---

## 九、第五轮：构建 1.2.0 并做免安装装机验收（2026-08-20）

**构建：** `scripts/build_windows_release.ps1`，`STORYLENS_RC_CANDIDATE=1`（本地 RC，跳过发布登记与 release-guard）。工具链齐全：cargo 1.91.0 · node 22.20.0 · PyInstaller 6.22.0 · @tauri-apps/cli 2.11.4。

产物：

| 文件 | 大小 |
|---|---|
| `StoryLens_1.2.0_x64-setup.exe` | 42.7 MB |
| `storylens-api.exe`（冻结 sidecar） | 39.7 MB |

**这是 1.1.2 之后第一个装机产物。** 构建脚本会临时改写 `tauri.conf.json` 再还原，实测工作区没被弄脏。

### 为什么是「免安装」而不是真装

机器上已装 **StoryLens 1.1.2**，安装目录是 `%LOCALAPPDATA%\StoryLens`——**和那个 920 MB 生产库是同一个目录**。装 1.2.0 会就地覆盖在用的 1.1.2，且新版首次启动会对生产库跑迁移。两件都不可轻易回退，所以按选择走免安装：直接跑构建出的 `storylens-desktop.exe`，用 `STORYLENS_DATA_DIR` 指向临时目录。

**生产库全程未被写入**——启动前后都是 964,657,152 字节、2026-08-15 14:48:42。

### 装机形态下验到了什么

| 项 | 结果 |
|---|---|
| 应用启动 | ✅ 进程存活，拉起自带 sidecar（端口 63042） |
| 数据目录隔离 | ✅ `STORYLENS_DATA_DIR` 生效，新库建在临时目录 |
| 迁移 | ✅ 全新库跑齐 **22 条**，含今天新增的 `021_book_analysis_form`；67 张表 |
| 导入 | ✅ 452 段，分章告警正确触发 |
| **导入时选长短篇** | ✅ `analysis_form=short` 随导入带过来，**今天的 CHG-120 在装机版里生效** |
| 首次运行的默认状态 | ✅ provider 默认未启用、云端总开关默认关——正确的隐私默认 |
| **冻结 sidecar 调模型** | ✅ `healthy`，76/5 token。这是 PyInstaller 打包最容易死的地方 |
| **完整短篇线** | ✅ 6 次调用 / 54 秒 / 21 段 / 起承转合 4 段 / 反复出现 10 条 / 13 段有呼应 |

### 装机验收查出的新缺陷（已修，4f1341fe）

**全新安装的头四秒，连续 4 次 500。** `GET /model-providers/{name}/configuration` 在 provider 行还不存在时抛 `ValidationError: 13 validation errors`。

代码本来有兜底——`row is None` 时构造一个临时 `ProviderConfiguration`——但 **SQLAlchemy 的列默认值只在 INSERT 时生效，构造时不生效**，于是每个字段都是 `None`。兜底存在，却不工作。

只有全新数据目录才会撞上，所以在 dev server 上跑一百遍也看不见——**这正是装机验收存在的理由**。

### 呼应计数器终于给出了答案

第三轮加的计数器在这次运行里记下：

```
callbacks_written=15  dropped_bad_target=0  dropped_bad_content=2
```

模型写了 15 条呼应，段号**全对**，只有 **2 条**因内容对不上被丢。所以此前 16 → 8 → 14 的波动，主因是**模型每次写出的条数不同**，不是校验在大量吃掉——这是加计数器之前无法区分的两种情况。

### 还剩什么

- **H2（真实数据升级）** 要覆盖 1.1.2 并对 920MB 生产库跑迁移，等你决定。
- **H3（断网）** 需要断掉这台机器的网络，我没有擅自做。
- **G3（检查更新按钮）** 只在装机版存在，免安装形态下没验；真装之后可一并验。

---

## 十、第六轮：用户实操暴露的三处缺陷（2026-08-20）

这一轮不是我走查出来的，是**你在装机版里实际用，然后截图问我**——三处我前五轮全没碰到。

### D5：界面要你确认一件已经做完的事，而且没有按钮（已修，64c3f8d0）

《长安的荔枝》第一章：运行 `succeeded`、审阅 `confirmed`、接受 13 条边界、14 个场景、14 条分析产物——**全做完了**。而面板一直显示「待确认场景划分」。刷新无效，因为不是客户端缓存旧了，是**接口本身自相矛盾**：

```
GET /analysis-runs/2
  status           = succeeded
  effective_status = awaiting_scene_boundary_confirmation
```

`effective_chapter_status` 会读一个写在运行上的标记。运行**走到**确认这一步时写下它；而确认动作只往同一份数据里写了自己的字段，**从不更新这个标记**——于是它在这次运行的余生里一直说「还在等」，并且这个判断排在所有实际证据之前。

同一个文件里早就有过一次同样的教训：CHG-013 的注释写着「正在跑的阅读旅程必须压过这个过期的等待标记」。**这次把同一条规则用在了更要紧的一种：已确认的审阅是比没人更新的标记更硬的证据。** 标记本身不动——它记录历史。

**没有按钮，恰恰是因为真的没东西可确认。** 界面在指使人去做一件不存在的事。

### D6：预算差三厘钱，页面给你一片空白

阅读旅程起不来。接口说得很清楚：

```
COST_BUDGET_INSUFFICIENT
  required  = 0.9488 元
  available = 0.945749 元
  shortfall = 0.003051 元
  user_message   = 今日费用预算不足
  settings_focus = budget
```

**接口连「该去哪调」都准备好了**（`settings_focus: budget`），页面一个字都没显示。

顺带一处可疑：同样这 14 个场景，**场景分析实际只花了 ¥0.054**，阅读旅程却预估 **¥0.9488**——差 17 倍。这个预估看起来是按最坏情况堆出来的，而它直接决定了「能不能开始」。

### D4：阅读旅程页签空白，且唯一的启动入口是静默空操作

把每日预算从 ¥1 抬到 ¥5 之后再试，接口返回：

```
HTTP 202  resume_stage = reader_journey
actions_executed = ['start_or_resume_reader_journey']
```

**但 `reader_journey_runs` 仍然是 0 条，也没有任何一次对外的模型调用。** 报告执行了，实际什么都没发生。

代码里对此有过记录——`BookRoutePage.tsx` 的 CHG-025 注释写着 recover「caused a silent no-op click」。**已知它会空转，但通往阅读旅程的恢复路径仍然只有它。**

场景确认面板上其实有两个按钮：「仅确认场景划分」和「确认这 N 个场景并开始分析」。走了前者，旅程就永远不会开始，而后面**没有一条能用的补救路径**。

### 这一轮的方法教训

**第四轮我宣布 D1 修好了，是只查了数据库、没看屏幕。** 那次的验证运行（run 28）带着一模一样的卡死标记——那一章在界面上同样会显示「待确认场景划分」。我报「跑通到 succeeded」时，屏幕上其实是错的。

**查行不等于查产品。** 这三处都是人真正去点才暴露的。
