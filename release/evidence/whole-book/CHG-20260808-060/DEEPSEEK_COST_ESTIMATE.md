# Pre-run Cost Estimate

Reuses Prepare → Cost Estimate → Consent → Create.

TOKEN ESTIMATE METHOD: heuristic (CJK×0.6 + Latin×0.3) on provider unit plan scale / existing window estimator; NOT whole-book chars alone.

Conservative: all input priced as cache miss.
MIN/MAX: output band (0.85×–1.25×) on DeepSeek path via provider_pricing.estimate_pre_run_cost_cny / cost estimate service.

UI disclaimer: 费用为预估值，实际收费以 DeepSeek 官方账单为准。
